name: Watch

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: pip install requests

      - name: Run watcher
        run: python watch.py
